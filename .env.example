# Environment
GEMINI_API_KEY=
# Optional. The live app persists summons in the browser Codex (localStorage).
# Prisma / Neon are scaffolded but not mounted on Express.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB
